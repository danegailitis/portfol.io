import { Componant } from 'react';
import firebase from 'firebase';

export default class YourStocks {
    constructor(){//props){
        //super(props);
        //this.userId = firebase.auth().currentUser.uid;
    }
    getUid(){
        alert(this.userId);
    }
}